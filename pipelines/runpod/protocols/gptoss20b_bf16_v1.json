{
  "protocol": "gptoss20b_bf16_v1",
  "comment": [
    "gpt-oss-20B served from unsloth/gpt-oss-20b-BF16. NOTE: openai does not",
    "ship an official bf16 checkpoint for gpt-oss-20b -- only the native MXFP4",
    "release (see gptoss20b_mxfp4_v1.json). This is a third-party dequantized",
    "upcast, not an Anthropic/openai-published artifact",
    "(notes/page_quant/studies/throughput_benchmark.md: 'there is no official",
    "bf16 gpt-oss checkpoint... not pulled, pending a decision on non-official",
    "sources'). Use this protocol when you specifically want the bf16-weights",
    "comparison point; use gptoss20b_mxfp4_v1.json for the checkpoint openai",
    "actually publishes.",
    "",
    "Calibration (rotations + VQ codebook) was captured from this SAME",
    "checkpoint (pipelines/oscar_e2e/build_gptoss.sh + build_gptoss_128k.sh,",
    "MODEL=unsloth/gpt-oss-20b-BF16 default) -- checkpoint-consistent by",
    "construction, unlike most of the historical mxfp4-bundle cells (see",
    "gptoss20b_mxfp4_v1.json's comment for that mismatch).",
    "",
    "vq2 = VQ for K (SGLANG_VQ_CODEBOOK_PATH, group-VQ gather kernel) + OSCAR",
    "SST rotation + int2 for V (SGLANG_OSCAR_V_ROTATION_PATH) -- this is what",
    "--vq2 always does in serve_oscar.sh, not a per-protocol choice; K's own",
    "OSCAR rotation/clip env is set but ignored by the VQ encoder.",
    "",
    "Three knobs below (moe_runner_backend, quant_group_size=0,",
    "absorb_v_rotation=0) are architecture-mandatory for gpt-oss's hybrid-SWA",
    "layout (12 of 24 layers full-attention; only those get a quant pool), not",
    "protocol choices -- see serve_oscar.sh:160-164 and",
    "notes/page_quant/studies/throughput_benchmark.md 'gpt-oss-20B integration'",
    "for the three boot blockers these work around (MoE routing NameError,",
    "fa3 hybrid-attn crash, absorb-rotation contiguous-layer assumption).",
    "TP=2 and every other serve knob below (max_reqs, kv_splits, cuda_graph_bs,",
    "mem_frac) match the validated recipe in logs/_scratch/boot_*.sh and the",
    "report14 SS10.5 shadow-fix study -- not re-derived here.",
    "",
    "Task grid is NIAH-only. gpqa_diamond.csv (raw GPQA, unformatted) and",
    "math500_gptoss_32k*.jsonl (question/solution pairs, no prompt/rid/",
    "max_new_tokens) are NOT client-ready rows -- run_prompts_client.py needs",
    "a pre-templated 'prompt' field per record, which only the niah_*_gptoss",
    "exports have. niah_131072 exists but is EXCLUDED: no cell has ever served",
    "gpt-oss above ctx=98304 (the throughput study's validated ceiling);",
    "serving 131072 needs ctx >131072, unvalidated on this stack. Extend ctx",
    "and add the task once that's been smoke-tested.",
    "",
    "Seed convention (user decision, 2026-07-28): NIAH uses 3 seeds.",
    "Sampling is temperature=1.0/top_p=1.0/top_k=-1 (disabled), per OpenAI's",
    "own gpt-oss repo: 'We recommend sampling with temperature=1.0 and",
    "top_p=1.0' (github.com/openai/gpt-oss README) -- NOT the Qwen protocol's",
    "T=0.6/top_p=0.95/top_k=20, which is a Qwen-specific convention that",
    "doesn't transfer. This supersedes the earlier greedy/single-sample",
    "choice (every historical gpt-oss NIAH cell in the repo was greedy;",
    "3-seed sampling over 1 is an explicit change, not an oversight). Rows",
    "are the 200-row balanced subsets (25/subtask, round-robin ordered) under",
    "artifacts/prompt_rows_proto/niah_<len>_gptoss_s200.jsonl -- built with",
    "openai/gpt-oss-20b's tokenizer, not unsloth's, and reused here: the only",
    "difference between the two checkpoints' rendered chat templates is a",
    "'Calls to these tools must go to the commentary channel' line unsloth's",
    "template adds unconditionally, which is inert since neither protocol",
    "ever defines a tool -- confirmed by direct template rendering, not",
    "assumed. Construction method reverse-engineered from Samuel's",
    "samuel_pull_20260727.zip (verified byte-identical against his Qwen s200",
    "files before reuse for gpt-oss).",
    "",
    "vq2 uses the CUDA stage-1 decode kernel (vq2_cuda_geom below), not the",
    "Triton kernel. Verified 2026-07-28: 800-row niah_8192 3-way comparison",
    "(baseline / Triton-optimized / CUDA) showed no accuracy regression on",
    "either optimized path (94.1/93.2/93.4 8-task avg, all deltas noise-level)",
    "and confirmed genuine kernel activation via the server log's",
    "'[vq2_cuda] CUDA stage-1 ACTIVE' line, not a silent Triton fallback.",
    "CAVEAT: the CUDA-kernel geometry generalization for gpt-oss (16,256,64,8",
    "-- Qwen's (32,256,128,4) was the only previously-supported geometry) is",
    "UNCOMMITTED in vendor/OSCAR-vq as of this protocol version -- this",
    "protocol has a hard dependency on engine working-tree state that isn't",
    "captured by the engine.sha in provenance.json. Commit it before treating",
    "any cell run against this protocol as reproducible from a clean clone.",
    "",
    "exact_chunked_prefill: false reproduces the published serving default.",
    "This is the SAME open decision as the Qwen protocol, but far more",
    "consequential here -- report14 SS10.4/10.5 measured healthy300 chunk-4096",
    "collapsing 89.00->67.33 (-21.7) without the shadow fix, driven ~3/4 by",
    "VQ-K and ~1/4 by INT2-V. Do not quote a chunked gpt-oss vq2 headline",
    "number without deciding this field first.",
    "",
    "Sync note: rotations/codebook and NIAH row files are both in manifest",
    "group 'gptoss' -- one sync covers this protocol."
  ],
  "model": "unsloth/gpt-oss-20b-BF16",
  "tp": 2,
  "ctx": 73728,
  "mem_frac": 0.80,
  "max_tokens_pool": "auto",
  "chunked_prefill_size": 4096,
  "exact_chunked_prefill": false,
  "cuda_graph_max_bs": 4,
  "moe_runner_backend": "triton",
  "quant_group_size": 0,
  "absorb_v_rotation": 0,
  "vq2_cuda_geom": "16,256,64,8",
  "rot_dir": "artifacts/oscar_gptoss20b/rotations_gpqa198",
  "vq_codebook": "artifacts/oscar_gptoss20b/vqa_gptoss20b_G4_strat_flat_ptn_gpqacc128k_fp8.pt",
  "arms": {
    "bf16":       { "mode_flag": "--bf16", "max_reqs": 4, "kv_splits": 8 },
    "oscar_int2": { "mode_flag": "",       "max_reqs": 4, "kv_splits": 8 },
    "vq2":        { "mode_flag": "--vq2",  "max_reqs": 4, "kv_splits": 8 },
    "quarot":     { "mode_flag": "--int2plain", "max_reqs": 24, "kv_splits": 8,
                     "hadamard_order": 64, "k_clip_ratio": 1.0, "v_clip_ratio": 1.0 },
    "turboquant_int2":  { "mode_flag": "--int2plain", "max_reqs": 24, "kv_splits": 8,
                     "hadamard_order": 64, "k_clip_ratio": 1.0, "v_clip_ratio": 1.0,
                     "lloyd_max": 1 },
    "turboquant_k3v3":  { "mode_flag": "--bf16", "max_reqs": 24, "kv_splits": 8,
                     "simquant_bundle": "artifacts/simquant/simquant_turboquant_d64_L12_k3v3.pt" }
  },
  "tasks": {
    "niah_8192":  { "rows": "artifacts/prompt_rows_proto/niah_8192_gptoss_s200.jsonl",  "samples": 3, "temperature": 1.0, "top_p": 1.0, "top_k": -1 },
    "niah_16384": { "rows": "artifacts/prompt_rows_proto/niah_16384_gptoss_s200.jsonl", "samples": 3, "temperature": 1.0, "top_p": 1.0, "top_k": -1 },
    "niah_32768": { "rows": "artifacts/prompt_rows_proto/niah_32768_gptoss_s200.jsonl", "samples": 3, "temperature": 1.0, "top_p": 1.0, "top_k": -1 },
    "niah_65536": { "rows": "artifacts/prompt_rows_proto/niah_65536_gptoss_s200.jsonl", "samples": 3, "temperature": 1.0, "top_p": 1.0, "top_k": -1 },
    "niah_smoke": { "rows": "artifacts/prompt_rows_proto/niah_8192_gptoss_s200.jsonl",  "samples": 1, "temperature": 0.0, "limit_rows": 24 },
    "math500_smoke": { "rows": "artifacts/prompt_rows/math500_gptoss_32k_n200.jsonl", "samples": 1, "temperature": 1.0, "top_p": 1.0, "top_k": -1, "limit_rows": 24 }
  }
}
